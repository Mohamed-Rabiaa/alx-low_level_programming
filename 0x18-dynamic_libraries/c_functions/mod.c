/**
 *mod - returns the reminder of a division of two integers
 *@a: the first integer
 *@b: the second integer
 *
 *Return: returns the reminder of a division of two integers
 */
int mod(int a, int b)
{
	return (a % b);
}
